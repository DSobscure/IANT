namespace IANTProtocol
{
    public enum StatusCode : byte
    {
        Correct,
        NotExist,
        InvalidOperation,
        InvalidParameter,
        PermissionDeny,
        Canceled
    }
}
